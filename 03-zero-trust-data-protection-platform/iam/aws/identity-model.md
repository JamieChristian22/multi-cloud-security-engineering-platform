# AWS IAM Model

Use federation or IAM Identity Center for humans and IAM roles for workloads. Require MFA for privileged access, avoid long-lived keys, scope permissions to specific resources, and review unused access regularly.
