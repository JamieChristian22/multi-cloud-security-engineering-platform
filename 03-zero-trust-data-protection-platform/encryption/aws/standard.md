# AWS Encryption Standard

Use SSE-KMS for sensitive S3 data, customer-managed KMS keys where required, automatic rotation, separate key administrators and users, CloudTrail monitoring, and TLS-only bucket policies.
