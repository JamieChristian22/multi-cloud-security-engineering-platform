title: AWS Root Account Login
description: Detects root account usage in AWS CloudTrail.
severity: high
log_source: aws_cloudtrail
detection:
  eventName: ConsoleLogin
  userIdentity.type: Root
mitre:
  tactic: Initial Access
  technique: Valid Accounts
response: Confirm authorization, rotate root credentials, enforce MFA, document evidence.
